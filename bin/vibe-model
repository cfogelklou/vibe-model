#!/bin/bash
# Vibe-Model CLI entry point
# This script runs the compiled TypeScript implementation using Bun

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bun "$SCRIPT_DIR/index.js" "$@"
