#!/bin/bash
# V-Model CLI entry point
# This script runs the TypeScript implementation using Bun

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bun run "$SCRIPT_DIR/../src/index.ts" "$@"
